KHỞI TẠO LOCALREPO :
GIT INIT

Tham chiếu đến remote repo :
git remote add origin <URL>

ADD tất cả :
git add .

ADD theo file
git add < file name>

Commit :
git commit -m "content"

Push :
git push origin <branch>